<Page
    x:Class="t_tracker_ui.Views.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24" RowSpacing="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/> 
        </Grid.RowDefinitions>

        <!-- App Title -->
        <StackPanel Spacing="8">
            <TextBlock Text="t_tracker" FontSize="28" FontWeight="SemiBold"/>
            <TextBlock Text="Focus and screen time tracker" FontSize="14" Opacity="0.7"/>
        </StackPanel>

        <!-- Info cards -->
        <StackPanel Grid.Row="1" Spacing="12">
            <Border Padding="12" CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <TextBlock Text="Version:" Width="100" FontWeight="SemiBold"/>
                    <TextBlock Text="{x:Bind AppVersion, Mode=OneWay}" FontFamily="Consolas"/>
                </StackPanel>
            </Border>

            <Border Padding="12" CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <TextBlock Text="Database:" Width="100" FontWeight="SemiBold"/>
                    <TextBlock Text="{x:Bind DbPath, Mode=OneWay}" TextWrapping="WrapWholeWords"/>
                </StackPanel>
            </Border>

            <Border Padding="12" CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <TextBlock Text="API base:" Width="100" FontWeight="SemiBold"/>
                    <TextBlock Text="{x:Bind ApiBase, Mode=OneWay}" FontFamily="Consolas"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- Footer -->
        <TextBlock Grid.Row="2"
                   Text="This application tracks active windows and provides REST endpoints for analysis."
                   Opacity="0.7"
                   TextWrapping="WrapWholeWords"/>
    </Grid>
</Page>
